{%- for sig in config['sigs'] %}
	KEY_{{ sig['name']|upper }},
    {%- for alg in sig['mix_with'] -%}
        {%- if alg['rsa'] %}
	KEY_{{ alg['name']|upper }}_{{ sig['name']|upper }},
        {%- else %}
	KEY_ECDSA_{{ alg['name']|upper }}_{{ sig['name']|upper }},
        {%- endif -%}
    {%- endfor %}
{%- endfor %}

