{%- for family, kexs in (config['kexs'] | selectattr("mix_with") | groupby('family')) %}
- **{{ family }}**: {%- for kex in kexs %}{% for curve in kex['mix_with'] %} `{{ curve['pretty_name'] }}`{% endfor %}{% endfor %}
{%- endfor %}

