{%- for family, kexs in (config['kexs'] | selectattr("family") | groupby('family')) %}
- **{{ family }}**: {% for kex in kexs -%}  `{% if family == 'ML-KEM' %}{{ kex['name']|replace('_','') }}-{{ kex['hash'] }}{% else %}{{ kex['name']|replace('_','-') }}-{{ kex['hash'] }}{% endif %}` {%- if kex['enable'] -%} \* {%- endif -%} {%- if not loop.last %}, {% endif -%}{%- if loop.last and family == 'HQC' -%}†{%- endif -%}{%- endfor -%}
{%- endfor %}

